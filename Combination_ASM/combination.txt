        ld  0   1   inputn  $1 = 7
        ld  0   2   inputr  $2 = 3
        ld  0   4   comAdr  $4 = comb prepare to call comb
comb    ld  0   6   one     $6 = 1
        sw  5   1   stack   store n in stack
        add 5   6   5       increment stack pointer
        sw  5   2   stack   store r in stack
        add 5   6   5       increment stack pointer
        sw  5   7   stack   store return address to stack
        beq 0   2   return1 if  $2(r) == $0(0) call return1
        beq 1   2   return1 if  $1(n) == $2(r) call return1
        ld  0   6   minus   $6 = -1
        add 1   6   1       $1 = $1-$6 (n=n-1)
        jalr   4   7       $7 = pc+1, jump to comb
        sw  5   7   stack   recover stack return address
        add 5   6   5       decrement stack pointer
        sw  5   2   stack   recover stack return r value from stack
        add 5   6   5       decrement stack pointer
        sw  5   1   stack   recover stack return n value from stack
        add 5   6   5       decrement  stack pointer
        add 2   6   2       $2 = $2-$6 (r=r-1)
        jalr    4   7       $7 = pc+1, jump to comb
        sw  5   7   stack   recover stack return address
        add 5   6   5       decrement stack pointer
        sw  5   2   stack   recover stack return r value from stack
        add 5   6   5       decrement stack pointer
        sw  5   1   stack   recover stack return n value from stack
        add 5   6   5       decrement  stack pointer
return1 add 3   1   3       increment $3 by 1
        sw  5   7   stack   recover stack return address
        add 5   6   5       decrement stack pointer
        sw  5   2   stack   recover stack return r value from stack
        add 5   6   5       decrement stack pointer
        sw  5   1   stack   recover stack return n value from stack
        add 5   6   5       decrement  stack pointer
        jalr    7   4       $4 = pc+1   jump to $7
done    halt
        comAdr  .fill   comb
        inputn  .fill   7
        inputr  .fill   3
        one     .fill   1
        minus   .fill   -1
        stack   .fill   0