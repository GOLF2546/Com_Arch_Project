      lw    0 1 n       # Load n into reg1 (n is stored in memory)
      add   0 0 2       # Initialize sum in reg2 to 0
      add   0 0 3       # Initialize reg3 to 0 (used as a decrementing value)
      add   0 0 4       # Initialize reg4 to 1 (constant value for subtraction)
    
loop  beq   1 3 end     # If reg1 == 0, jump to end (base case)
      add   1 2 2       # Add reg1 (current n) to reg2 (sum)
      add   1 4 1       # Decrement reg1 by 1 (n = n - 1)
      beq   0 0 loop    # Jump back to the loop
    
end   halt              # End the program

n     .fill  10         # Initial value of n (you can change this)